/**
 * Main program entry point.
 *
 * @param  argc the argument count
 * @param  argv c-style string containing program arguments
 * @return      program status; 0 means no errors, anything else is bad
 */
int main(int argc, char** argv);